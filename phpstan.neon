parameters:
    level: 8
    paths:
        - src
    excludePaths:
    ignoreErrors:
        # UART constants are part of the W65C51N specification
        - '#Constant Emulator\\UART::STATUS_FRAMING_ERROR is unused#'
        - '#Constant Emulator\\UART::STATUS_OVERRUN_ERROR is unused#'
        # UART internal state management
        - '#Property Emulator\\UART::\$transmitData is never read, only written#'
        - '#Property Emulator\\UART::\$irqEnabled is never read, only written#'
    tmpDir: /tmp/phpstan
